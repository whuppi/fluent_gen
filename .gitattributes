# Golden fixtures are compared byte-for-byte against the generator's
# output, which is always LF. Freeze their line endings so Windows
# autocrlf can't rewrite them on checkout and break the comparison.
test/emission/golden/*.ftl           -text
test/emission/golden/*.expected.dart -text
